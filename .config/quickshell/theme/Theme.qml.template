pragma Singleton
import QtQuick

QtObject {
    // Backgrounds
    property color background: "{{colors.surface.default.hex}}"
    property color barBackground: "{{colors.surface_container.default.hex}}"
    
    // Accents
    property color accent: "{{colors.primary.default.hex | set_saturation: 100}}"
    property color secondary_accent: "{{colors.secondary.default.hex | set_saturation: 100}}"
    property color tertiary_accent: "{{colors.tertiary.default.hex}}"
    property color occupied: "{{colors.tertiary.default.hex}}"
    property color accent_down: "{{colors.primary_fixed_dim.default.hex}}"
    property color empty: "{{colors.surface_variant.default.hex}}"
    
    property color text: "{{colors.on_surface.default.hex}}"

    property string logoPath: "file:///home/larke/.config/quickshell/assets/logo-arch-linux-icon.png"
}
