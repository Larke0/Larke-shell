general {
    no_fade_in = false
    grace = 0
    disable_loading_bar = true
}

background {
    monitor =
    path = ~/.config/hypr/assets/wallpaper
    
    # Blur options
    blur_passes = 1
    blur_size = 1
    noise = 0.0117
    contrast = 0.8916
    brightness = 0.8172
    vibrancy = 0.1696
    vibrancy_darkness = 0.0
}

input-field {
    monitor =
    size = 300, 50
    outline_thickness = 3
    dots_size = 0.4
    dots_spacing = 0.15 
    dots_center = true
    dots_rounding = -1 
    
    # Outer Ring: Primary Color with 'ee' alpha (transparency)
    outer_color = rgba(adc6ffee)
    
    # Inner Background: Surface Color (Solid)
    inner_color = rgba(111318ff)
    
    # Text Color: On Surface (Solid)
    font_color = rgba(e2e2e9ff)
    
    fade_on_empty = true
    fade_timeout = 1000 
    placeholder_text = <i>Input Password...</i> 
    hide_input = false
    rounding = -1 
    
    # Check Color: Tertiary (Solid)
    check_color = rgba(debcdfff)
    
    # Fail Color: Error (Solid)
    fail_color = rgba(ffb4abff)
    
    fail_text = <i>$FAIL <b>($ATTEMPTS)</b></i> 
    fail_transition = 300 
    
    position = 0, 50
    halign = center
    valign = bottom
}

label {
    monitor =
    # Clock
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    
    # Clock Color: On Surface (Solid)
    color = rgba(e2e2e9ff)
    
    font_size = 100
    font_family = Noto Sans
    position = 0, 80
    halign = center
    valign = center
}


label {
    monitor =
    text = 
        
    color = rgba(e2e2e9ff)
    
    font_size = 27
    font_family = JetBrainsMono Nerd Font Propo
    position = -35, 32 
    halign = right
    valign = bottom
}

shape {
    monitor =
    size = 60, 60
    color = rgba(111318ff)
    rounding = -1 # circle
    border_size = 4
    border_color = rgba(adc6ffee)

    position = -23, 21
    halign = right
    valign = bottom

    onclick = systemctl suspend
}



label {
    monitor =
    text = 󰜉
        
    color = rgba(e2e2e9ff)
    
    font_size = 40
    font_family = JetBrainsMono Nerd Font Propo
    position = -130, 23
    halign = right
    valign = bottom
}

shape {
    monitor =
    size = 60, 60
    color = rgba(111318ff)
    rounding = -1 # circle
    border_size = 4
    border_color = rgba(adc6ffee)

    position = -114, 21
    halign = right
    valign = bottom

    onclick = systemctl reboot
}


label {
    monitor =
    text = 󰐥
    
    color = rgba(e2e2e9ff)
    
    font_size = 40
    font_family = JetBrainsMono Nerd Font Propo
    position = -220, 20
    halign = right
    valign = bottom
}

shape {
    monitor =
    size = 60, 60
    color = rgba(111318ff)
    rounding = -1 # circle
    border_size = 4
    border_color = rgba(adc6ffee)

    position = -205, 21
    halign = right
    valign = bottom

    onclick = systemctl poweroff
}
